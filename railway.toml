[build]
dockerfilePath = "Dockerfile"

[deploy]
# GET /api/privacy es público y no requiere BD activa — perfecto para healthcheck
healthcheckPath = "/api/privacy"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
